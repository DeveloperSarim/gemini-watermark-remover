## Workspace Notes

- Vanilla Vite app in plain JavaScript.
- The UI embeds `https://geminiwatermarkremover.io/` in an iframe and offers a fallback open-in-new-tab flow.
- Keep the project lightweight and avoid reintroducing React or TypeScript unless the user asks for it.